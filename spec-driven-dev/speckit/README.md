Legacy Speckit instructions and templates for repositories that still use the older
`specs/<feature-id>/` workflow.

This repository now uses OpenSpec under `openspec/` and has retired the repo-root
`specs/` directory. Keep this folder only for cross-repo legacy assistance; do not use
it for current planning in this repo.

interface:
  display_name: "Test and Log"
  short_description: "Test without modifying code, log findings"
  default_prompt: "Use $test-and-log to run <subject>, document each step's outcome, and write a structured log to context/logs."

policy:
  allow_implicit_invocation: false

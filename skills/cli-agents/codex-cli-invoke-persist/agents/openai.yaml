interface:
  display_name: "Codex CLI Invoke Persist"
  short_description: "Persist and resume Codex sessions"
  default_prompt: "Use $codex-cli-invoke-persist to create (persist) and resume Codex CLI sessions via the `codex` CLI."

policy:
  allow_implicit_invocation: false
